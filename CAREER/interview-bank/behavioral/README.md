# Behavioral Questions

每个文件 = 一道行为题（含 STAR 答案）。命名 `{question-slug}.md`（如 `tell-me-about-supervisor-agent-project.md`）。

STAR 答案的"原始素材"来自 `PROJECTS/work/<实习>/` 的复盘页 —— 你和 LLM 对话挖掘实习时产出的项目页。这里只是把原始素材**重写为面试可讲述的版本**。

模板见父目录 `../README.md`。
